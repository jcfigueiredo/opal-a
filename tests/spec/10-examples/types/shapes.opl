export { Shape, area }

enum Shape
  Circle(radius: Float)
  Rectangle(width: Float, height: Float)
end

def area(shape)
  match shape
    case Shape.Circle(r)
      3.14 * r * r
    case Shape.Rectangle(w, h)
      w * h
  end
end
