class Product
  needs name: String
  needs price: Float
  needs weight: Float

  def describe()
    f"{.name} (${.price}, {.weight}kg)"
  end

  def get_name()
    .name
  end

  def get_price()
    .price
  end

  def get_weight()
    .weight
  end
end
