# expect: small positive | negative
def classify(n)
  match n
    case x if x > 0 and x < 10
      "small positive"
    case x if x < 0
      "negative"
    case _
      "other"
  end
end
print(f"{classify(5)} | {classify(-3)}")
