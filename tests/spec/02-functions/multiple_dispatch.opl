# expect: (hi) | (hi, w=10) | (hi, w=10, a=left)
def format(val)
  f"({val})"
end

def format(val, width)
  f"({val}, w={width})"
end

def format(val, width, align)
  f"({val}, w={width}, a={align})"
end

print(f"{format("hi")} | {format("hi", 10)} | {format("hi", 10, "left")}")
