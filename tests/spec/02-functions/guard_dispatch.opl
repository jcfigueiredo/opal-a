# expect: positive: 5 | generic: -3 | generic: hello
def process(value)
  f"generic: {value}"
end

def process(value: Int)
  requires value > 0
  f"positive: {value}"
end

r1 = process(5)
r2 = process(-3)
r3 = process("hello")
print(f"{r1} | {r2} | {r3}")
