# expect: Withdrew 50
macro guard(condition, message)
  ast
    if not ($condition)
      raise $message
    end
  end
end

def withdraw(amount)
  @guard amount > 0, "amount must be positive"
  print(f"Withdrew {amount}")
end

withdraw(50)
