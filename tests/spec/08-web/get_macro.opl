# expect: macro defined
extern "http"
  def create_app() -> App
  def add_route(app, method: String, path: String, handler: Fn) -> Null
end

app = create_app()

macro get(path, handler)
  ast
    add_route(app, "GET", $path, $handler)
  end
end

@get "/", |req| "Hello!"

print("macro defined")
