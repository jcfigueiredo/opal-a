# expect: LOG: hello | LOG [WARN]: hello
class Logger
  needs prefix: String

  def log(msg)
    f"{.prefix}: {msg}"
  end

  def log(msg, level)
    f"{.prefix} [{level}]: {msg}"
  end
end

l = Logger.new(prefix: "LOG")
print(f"{l.log("hello")} | {l.log("hello", "WARN")}")
